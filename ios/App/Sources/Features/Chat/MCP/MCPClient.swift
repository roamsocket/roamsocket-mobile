// Placeholder deleted. The MCPClient mock (sleeps + fake data) was removed
// in the bogus cleanup pass. Real MCP servers are configured via the
// settings sheet (`MCPManagerView`) and invoked by the desktop server
// during a coding session. The iOS app itself does not call MCP directly.
