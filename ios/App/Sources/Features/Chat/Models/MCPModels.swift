// Stub MCP types removed. The mock MCPClient that populated these models
// was deleted in the bogus cleanup pass. Real MCP server configuration
// lives in `MobileAICore/Sources/MobileAICore/Skills/MCPServer.swift` and
// is surfaced via the `MCPManagerView` settings sheet.
import Foundation
