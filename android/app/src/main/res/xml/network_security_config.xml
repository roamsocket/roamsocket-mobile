<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext is allowed only to LAN / loopback hosts so the Android app can
     pair with the local desktop server, mirroring iOS NSAppTransportSecurity.
     Tighten before Play Store distribution. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- RFC1918 private ranges — common LAN addresses for the desktop. -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>
</network-security-config>
