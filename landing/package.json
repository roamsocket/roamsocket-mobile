{
  "name": "codesocket-landing",
  "private": true,
  "description": "CodeSocket marketing site — static assets on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  }
}